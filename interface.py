import logic
#TODO gui interface

#intext = logic.processInput(input("Give it to me: "))
#translationMode = input("Choose a translation mode! (binary = 101000, classic = .-...-, typed = e)")
#print(logic.parseMorse(intext, translationMode))
print(logic.processInput('.-.-..- .-.-.- --.    --.- ..-.'))
